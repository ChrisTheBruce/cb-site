/** @type {import('tailwindcss').Config} */
export default {
  darkMode: 'class',
  content: ['./**/*.{html,js,jsx,ts,tsx}'],
  theme: { extend: {} },
  plugins: [],
  // Optional: if you use dynamic class names, add a safelist:
  // safelist: ['aspect-video','bg-gradient-to-tr','from-blue-100','via-sky-100','to-indigo-100'],
}
